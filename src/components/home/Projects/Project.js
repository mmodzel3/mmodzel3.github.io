import CardElement from "../../widgets/lists/CardList/Card/CardElement";

class Project extends CardElement {
    constructor(title, description, link, image, technology) {
        super(title, description, link, image, technology);
    }
}

export default Project;